#!/usr/bin/env bash
# Run asb without requiring a global install.
ROOT="$(cd "$(dirname "$0")" && pwd)"
if [[ -x "$ROOT/.venv/bin/asb" ]]; then
  exec "$ROOT/.venv/bin/asb" "$@"
fi
export PYTHONPATH="$ROOT${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m agent_session_bridge "$@"
